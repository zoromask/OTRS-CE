apiVersion: v1
kind: Service
metadata:
  name: otrs
  namespace: otrs
  labels:
    app: otrs
spec:
  type: LoadBalancer
  selector:
    app: otrs
  ports:
    - name: http
      port: 80
      targetPort: 80
